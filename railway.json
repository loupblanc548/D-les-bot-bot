{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "node --expose-gc --import tsx src/index.ts",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300
  }
}
