name: "CodeQL Config"

# Disable queries that are false positives for this project's design
# These are inherent patterns (audio temp files, auth tokens in API calls)
# that cannot be avoided without breaking functionality.

paths_ignore:
  - desktop-app/js/dev-mode.js
  - desktop-app/js/enhancements.js
