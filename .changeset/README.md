# Changesets

This file is used by changesets to track changes.
When you run `npx changeset`, a new file will be created in this folder.
When you run `npx changeset version`, the version is bumped and the CHANGELOG is updated.
